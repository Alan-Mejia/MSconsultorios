server:
  port: 8090

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Alan-Mejia/MSconsultorios.git
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
          search-paths: config-data
          default-label: master
