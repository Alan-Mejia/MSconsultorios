server:
  port: 8090

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Alan-Mejia/MSconsultorios.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: s3cr3t